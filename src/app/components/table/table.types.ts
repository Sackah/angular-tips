export type Employee = {
  firstName: string;
  lastName: string;
};

export type Inventory = {
  plu: number;
  supplier: string;
  name: string;
  inStock: number;
  price: number;
  currency: string;
};
